# Rust build artifacts (never commit).
target/

# Local-only env (never commit; see .env.example).
.env
.env.local

# Rendered live configs (generated by `openbutler-setup init`; see configs/*.example).
configs/voice.json
configs/face.json
configs/board.json

# Runtime bus + board state + logs (all recreated at runtime).
bus/
state/state
state/wave.json
state/mood.json
logs/

# Local-only tracker + port notes (live here, never in the repo).
docs/

# Editor / OS droppings (never commit).
.DS_Store
*~
*.swp
*.orig
*.rej
.vscode/
.idea/
